# A passage worth finding

This is a sample document. Edit it, switch to Preview, or try Split view.

## Jump straight to the point

- Read the first item.
- Read this item next.
  - This nested item has its own source line.

The highlighted passage stays visible after the animation finishes.

## Edit the source

```typescript
type Result = { ok: boolean; message: string };

export function describe(result: Result): string {
  return result.ok ? "All checks passed" : result.message;
}
```

## Keep your changes

Use **Download copy** to export the edited source. A generated page does not change its original file.

Use **Open file** to read another Markdown or code file from disk. In browsers with file-saving support, **Save** writes to the file you explicitly picked.

## Compare details

| View | Use it for |
| --- | --- |
| Preview | Read formatted Markdown |
| Edit | Change the source |
| Split | Read and edit together |

> Files stay in your browser. This viewer does not upload them.
